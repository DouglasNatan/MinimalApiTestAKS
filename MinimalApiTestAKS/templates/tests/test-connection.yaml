apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "minimalapitestaks.fullname" . }}-test-connection"
  labels:
    {{- include "minimalapitestaks.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "minimalapitestaks.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
